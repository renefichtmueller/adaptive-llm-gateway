## What
Briefly: what does this PR change?

## Why
Link to issue or short rationale.

## How
Notes on implementation choices. Surprises, breaking changes, anything reviewers need to know.

## Checklist
- [ ] Build passes (`npm --workspace=packages/gateway run build`)
- [ ] Lint passes (`npx eslint packages/*/src`)
- [ ] Tests pass / added (`npm --workspace=packages/gateway test`)
- [ ] No secrets, IPs, or private hostnames committed
- [ ] README / ACKNOWLEDGMENTS updated if user-facing
- [ ] Build-drift guard happy (no stale `dist/`)
