# ─── Routing rules ────────────────────────────────────────────────────────
# Maps task_type → model + tier + prompt_template + fallback chain.
# The gateway picks the entry that best matches the caller's request.
#
# Add your own task_types here. The pre-classifier will route into them
# automatically based on the prompt content.

rules:
  # ─── Built-in: generic tasks ────────────────────────────────────────────
  pre_classify:
    model: qwen2.5:3b
    tier: fast
    prompt_template: pre_classifier
    description: "Lightweight classifier called internally before routing"

  generic_qa:
    model: qwen2.5:14b
    tier: medium
    prompt_template: pre_classifier
    description: "Default for any conversational Q&A"

  generic_summarize:
    model: qwen2.5:14b
    tier: medium
    prompt_template: pre_classifier
    description: "Summarize a longer text into 2-5 sentences"

  generic_extract:
    model: qwen2.5:14b
    tier: medium
    prompt_template: pre_classifier
    description: "Extract structured data from unstructured input"

  generic_classify:
    model: qwen2.5:3b
    tier: fast
    prompt_template: pre_classifier
    description: "Single-label or multi-label classification"

  generic_rewrite:
    model: qwen2.5:14b
    tier: medium
    prompt_template: pre_classifier
    description: "Style transfer / tone adjustment / rephrase"

  # ─── Built-in: code tasks ───────────────────────────────────────────────
  code_review:
    model: qwen2.5-coder:7b-instruct
    tier: fast
    prompt_template: pre_classifier
    description: "Quick review pass on a code diff"

  code_generate:
    model: qwen2.5:32b
    tier: large
    prompt_template: pre_classifier
    description: "Generate new code from a spec"

  code_generation:
    model: qwen2.5:32b
    tier: large
    prompt_template: pre_classifier
    description: "Code generation via static routing (the dynamic scorer routes to the Codex bridge)"

  # ─── Built-in: translation ──────────────────────────────────────────────
  content_translation_de_en:
    model: qwen2.5:14b
    tier: medium
    prompt_template: pre_classifier
    description: "Translate German → English preserving formatting"

  content_translation_en_de:
    model: qwen2.5:14b
    tier: medium
    prompt_template: pre_classifier
    description: "Translate English → German preserving formatting"

  # ─── Built-in: gateway internals ────────────────────────────────────────
  internal_ban_detect:
    model: qwen2.5:3b
    tier: fast
    prompt_template: internal_ban_detect
    description: "Banlist trigger detection on outputs"

  internal_prompt_improve:
    model: qwen2.5:14b
    tier: medium
    prompt_template: internal_prompt_improve
    description: "Self-improvement: suggest better prompt for a poor result"

# Fallback chains live in models.yaml (fallback_chains:).
